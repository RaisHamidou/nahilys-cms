export interface BubbleColorMenuItem {
    name: string;
    color: string;
}
export declare const ColorSelector: ({ open, onOpenChange }: {
    open: boolean;
    onOpenChange: (open: boolean) => void;
}) => import("react/jsx-runtime").JSX.Element | null;
